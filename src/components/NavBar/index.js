import "./navbarStyle.css";
import { Link, NavLink } from "react-router-dom";

const NavBar = ({ user }) => {
  return (
    <div className="navbar">
      <h1>NavBar</h1>
    </div>
  );
};

export { NavBar };
